<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout
xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:padding="16dp">

<!-- Boş liste durumunda mesaj göstermek için -->
<TextView
    android:id="@+id/txtEmptyMessage"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerInParent="true"
    android:visibility="gone"
    android:text="notravels"
    android:textSize="18sp"
    android:textColor="#808080" />

<!-- Seyahat listesi için RecyclerView -->
<androidx.recyclerview.widget.RecyclerView
    android:id="@+id/recyclerView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@+id/btnAddTravel"
    android:clipToPadding="false"
    android:paddingBottom="16dp" />

<!-- Yeni seyahat eklemek için bir Button -->
<Button
    android:id="@+id/btnAddTravel"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:layout_centerHorizontal="true"
    android:layout_marginBottom="16dp"
    android:text="addtravel"

    android:textColor="#FFFFFF" />
</RelativeLayout>